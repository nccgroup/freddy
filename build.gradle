
plugins {
    id 'java'
    id 'idea'
}

compileJava.options.encoding = 'UTF-8'

repositories {
    mavenCentral()
}

dependencies {
    implementation 'net.portswigger.burp.extender:burp-extender-api:1.7.22'
    testImplementation 'junit:junit:4.12'
    testImplementation 'org.powermock:powermock-module-junit4:2.0.5'
    testImplementation 'org.powermock:powermock-api-mockito2:2.0.5'
    implementation group: 'com.esotericsoftware', name: 'minlog', version: '1.3.1'
}

sourceCompatibility = 1.11
targetCompatibility = 1.11

// deviates from standard Gradle directory structure
sourceSets.main.java.srcDirs = ['src']
sourceSets.test.java.srcDirs = ['test']

version = '2.2.5'
group 'trust.nccgroup'

jar {
    from { configurations.compileClasspath.collect { it.isDirectory() ? it : zipTree(it) } }

}

